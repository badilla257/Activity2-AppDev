import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Act2-IonicApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
